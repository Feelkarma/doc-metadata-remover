# Cleaning modern Office files (.docx, .pptx, .xlsx) uses ONLY the Python
# standard library (zipfile, xml, re, tkinter) — no third-party packages needed.

# Optional: enables drag-and-drop of files onto the GUI window.
tkinterdnd2>=0.3.0

# Optional: only needed to build a standalone executable.
pyinstaller>=6.0

# Optional: enables cleaning legacy binary Office files (.doc, .ppt, .xls).
# Tiny, pure-Python, no external programs required. If not installed, the app
# still runs and cleans everything else — legacy files are simply reported as
# unsupported until this is installed.
olefile>=0.46

# Optional: enables PDF metadata removal (.pdf). If not installed, the app
# still runs and cleans everything else — PDFs are reported as unsupported
# until this is installed.
pikepdf>=8.0
